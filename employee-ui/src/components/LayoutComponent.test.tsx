import React from "react";
import { render, fireEvent, findByText } from "@testing-library/react";

import LayoutComponent from "./LayoutComponent";

describe("Layout tests", () => {
    it("should layout component load", () => {

    });

    it("should handle upload tab click", () => {

    });

    it("should handle employee tab click", () => {

    });

    it("should collapse and expand sider with button click", () => {

    });
});