

describe('Employee Service', () => {
    it('Should add employee to database', () => {
        expect(1).toBe(1);
    });

    it ("Should return success if all records added successfully", () => {
        expect(1).toBe(1);
    });

    it('Should return error if same id available in records', () => {
        expect(1).toBe(1);
    });

    it('Should return error if same login available in records', () => {
        expect(1).toBe(1);
    });

    it('Should return error if missing fields in records', () => {
        expect(1).toBe(1);
    });

    it('Should ignore record if record start with #', () => {
        expect(1).toBe(1);
    });
});